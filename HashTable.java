public class HashTable {
    private HashedList[] hashArray;
    private int arraySize;

    HashTable(int arraySize) {
        this.arraySize = arraySize;
    }

//    public void insert(int key) {
//        int hashVal = hashFunc(key);
//        hashArray[hashVal].insert(link);
//    }
}
